---
const { class: className, ...props } = Astro.props;
---

<div
  class:list={[
    "rounded-lg border bg-card text-card-foreground shadow-2xs",
    className,
  ]}
  {...props}
>
  <slot />
</div>
